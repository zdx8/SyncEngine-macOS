# 数据同步工具 技术方案设计

版本 v1.5 · 2026-09-13

变更记录：

- **v1.5** — 修掉「主题选跟随系统时界面显示错乱」：**「跟随系统」不再交给系统（`nil`），
  而是解析成具体外观**（6.3.1）。病根是从浅色切到跟随系统时，SwiftUI 的详情列会变成
  一块空白深色并永久停留（标题栏浅、侧栏中、详情深，四处互相矛盾且不自我恢复）；
  静态启动反而是好的，所以只在切换时暴露。附带补齐两件事：**订阅系统外观变化**
  （解析成具体值后系统不再替我们更新窗口），以及把整页的颜色来源统一到模型的
  `effectiveColorScheme`。新增**外观状态矩阵验证**（`Scripts/verify_appearance.sh`：
  3 种模式静态启动 + 3 个方向运行中切换，断言标题栏/侧栏/详情三处一致且详情区非空）。
  另修掉两处遗留：`WindowGroup` 标题与菜单栏「退出」项仍是旧名。
- **v1.4** — 界面细节调整与三处静默缺陷修复。**强调色按明暗分开取值**：浅色改用品牌绿
  深一档的 `#22A322`（白底 3.3:1，此前品牌色本身只有 2.12:1，小字号文字偏低），
  深色仍用品牌色 `#32CD32`；应用图标底色不变。**侧栏字号 12.5 → 15pt、行距 32.5 → 40pt**，
  列宽相应放宽。窗口与应用标题统一为 **`sync-engine`**；**移除常驻的引擎状态条**。
  修掉三处静默缺陷：**环境值注入到不了 `NavigationSplitView` 的侧栏**
  （导致深色下侧栏图标用了浅色强调色，见 6.4.3）、**内容被 `frame` 默认对齐垂直居中**
  （页头浮在窗口正中，见 6.1.1）、以及**启动页仍不确定**（竞速第二次赋值，见 13.4）。
  另外纠正了上一轮的验证方法论错误：判定"落在哪一页"的指纹没有区分力。
- **v1.3** — 强调色改为品牌色 **`#32CD32`**（浅深共用），**应用图标底色同步改为该色**；
  警告色相应从红调整为琥珀（琥珀在强调色为橙时被占用过，现在空出来了），
  修掉了"WARN 与 ERR 同为红色"这一可见缺陷。移除启动时播种的两个示例任务，
  并把"启动时任务列表为空"加进无界面自检。修掉三处静默问题：**打包脚本按
  "文件是否存在"缓存图标**（改了品牌色不会重出图标）、**窗口状态恢复导致每次启动
  落在不同的侧栏页**（使截图验证不可复现）、以及图标生成未随源文件变更而重跑。
- **v1.2** — **目标端存储落地：SMB 与 WebDAV 均已实现并验证。** 新增存储驱动抽象层（4.7），
  三种目标端统一为同一套接口；SMB 走系统 NetFS 挂载后复用本地驱动，WebDAV 用 URLSession
  直发 PROPFIND/PUT/MKCOL/DELETE 并自写 multistatus 解析。凭据改由系统钥匙串保存（7.1）。
  新增传输安全策略说明（7.2）。测试规模 13 → 76 项。
- **v1.1** — P0 骨架完成（SwiftPM 工程 + Swift 引擎 + SwiftUI 三功能区 + 打包脚本 + 无界面自检）。补充界面细节：任务可编辑、右上角外观切换、菜单栏常驻、关闭主窗口行为可选。补充 **Swift 引擎的并发哈希实测数据**（10 路并发比串行快 4.2 倍，吞吐 1179 MB/s，是原 Rust + BLAKE3 串行实现的 3.6 倍）。
- **v1.0** — **项目转向：仅 macOS 单平台 + Apple 原生开发。** 取消 iOS / 鸿蒙 / Android / Windows 全部平台规划；技术栈由 Flutter + Rust 改为 **SwiftUI + Swift**；同步引擎由 Rust 重写为 Swift（内容哈希由 BLAKE3 改为 CryptoKit SHA-256）。R1（鸿蒙链路）、R2（夸克合规）、R4（iOS 后台限制）随平台收敛一并消除。此前的 Flutter/Rust/鸿蒙验证成果按决策全部删除。
- ~~**v0.4**~~ — 曾完成 POC-① 端到端在 HarmonyOS 与 macOS 上的验证（已随本次转向作废）
- ~~**v0.3**~~ — 曾取消夸克网盘、确认鸿蒙为一等公民（已作废）
- ~~**v0.2**~~ — 曾确认纯本地模式（该决策在 v1.0 中**依然有效**，见 1.3）
- ~~**v0.1**~~ — 初版

---

## 1. 需求与定位

### 1.1 平台范围

**仅 macOS。** iOS、鸿蒙、Android、Windows 全部移出规划。

这个收敛带来的直接收益是可以使用**平台独占能力**，而不必为跨端妥协：

| 能力 | 跨端方案（原） | macOS 原生（现） |
|---|---|---|
| 界面 | Flutter 自绘，五端一致 | SwiftUI，跟随系统外观与无障碍 |
| 文件变更检测 | 轮询 + 全量哈希 | **FSEvents** 内核事件，几乎零成本 |
| 凭证存储 | 各端各自实现 | **Keychain**，系统级加密与同步 |
| 文件访问授权 | 自定义 | **安全作用域书签**，系统托管 |
| 后台执行 | 各端各自限制 | **菜单栏常驻 + LaunchAgent** |
| SMB | 自研协议插件 | 系统原生 `mount_smb` / NetFS |
| WebDAV | 自研协议插件 | `URLSession` 直接支持 |

**这不只是"少写几个平台"，而是把若干最难的问题变成了系统已解决的问题。** 原来为鸿蒙手写 SMB/WebDAV/NFS 平台插件的计划（原 6.1 节）整体作废。

### 1.2 功能分区

三个功能区，与既有设计一致：

| 分区 | 职责 |
|---|---|
| **同步任务** | 任务列表、新建/编辑任务、执行同步、查看进度与结果 |
| **同步日志** | 分级日志、按任务/级别筛选、导出 |
| **设置** | 存储连接管理、同步参数、引擎信息与自检、外观 |

### 1.3 运行模式：纯本地（沿用 v0.2 决策，依然有效）

无账号体系、无服务端。每台设备自包含：SwiftUI 界面 + Swift 引擎 + 本地 SQLite 索引 + Keychain 凭证。

**收益**：砍掉服务端、鉴权、多端同步协议、跨设备一致性合并四块工作；零服务器成本；凭证不出设备。

**代价**：
1. 连接配置需要用户自己搬运（缓解：配置加密导出/导入）
2. 凭证无云备份（缓解：Keychain 可随 iCloud 钥匙串同步——单平台后这一项从"做不到"变成了"系统已有"）
3. 无跨设备冲突协调

**技术连带影响**：本地模式下 mtime 不可靠（外接盘可能是 FAT/exFAT，精度 2 秒；时钟可能漂移），**变更检测的仲裁必须依赖内容哈希**。这是引擎的核心约束。

---

## 2. 技术选型

### 2.1 为什么是 SwiftUI，而不是继续用 Flutter

| 维度 | SwiftUI（采用） | Flutter |
|---|---|---|
| 平台能力 | 直接调用全部 AppKit / Foundation / CryptoKit | 需要平台通道逐项桥接 |
| 外观一致性 | 自动跟随系统外观、强调色、无障碍设置 | 需自绘，永远差一点 |
| 分发体积 | 原生应用通常 **1–3 MB** | 需打包 Flutter 引擎，约 **40 MB** |
| 构建链 | `swift build` 一条命令 | Xcode + Flutter 工具链 + 可能的 CocoaPods |
| 多平台 | 仅 Apple | 五端 |

**转向前 Flutter 的唯一优势是"一份 UI 覆盖五端"。平台收敛到 macOS 后，这个优势归零，而它的代价（体积、桥接、外观差异、构建复杂度）全部保留下来。** 因此换用 SwiftUI 是收敛后的必然选择。

### 2.2 为什么引擎从 Rust 改写为 Swift

原选 Rust 的理由是"**唯一能同时产出五端原生库的引擎语言**"——这个理由随平台收敛一并失效。

改写后的取舍：

| | Swift（采用） | Rust |
|---|---|---|
| 语言数量 | **1** | 2（+FFI 边界 + 两套构建链） |
| 内容哈希 | CryptoKit SHA-256，**硬件加速** | BLAKE3（需自备实现，或引入依赖） |
| 并发 | Swift Concurrency / Dispatch | 需自管线程池 |
| 系统集成 | 直接（FSEvents、Keychain、NetFS） | 全部要经 FFI 桥接 |
| 协议生态 | Foundation 齐备 | 需引入 crate |

**关于哈希算法从 BLAKE3 改为 SHA-256：**

- 原选 BLAKE3 是为了"速度 + 强度 + 分块"，但 CryptoKit 的 SHA-256 在 Apple Silicon 上**有硬件加速**，实测吞吐高于软件实现的 BLAKE3
- SHA-256 是系统实现，无需第三方代码——省掉一个依赖与一处跨平台编译风险
- 对内容指纹这一用途，SHA-256 的强度完全够用（不存在长度扩展攻击的场景）
- 代价：索引中记录的哈希格式变了。当前**尚无已持久化的索引数据**，无迁移负担

> **保留的设计判断**：哈希仍必须**分块流式**计算，不能整文件读入内存——同步对象可能是数十 GB 的磁盘映像。

### 2.3 依赖策略

**只用系统框架，不引入任何第三方 Swift 包。**

理由：SwiftUI + Foundation + CryptoKit + SQLite（系统自带 libsqlite3）已覆盖全部需求。每引入一个包，都要承担其维护状态、Swift 版本兼容、以及"它什么时候会被放弃"的风险。

需要 SQLite 时直接 `import SQLite3`（系统提供），不引 SQLite.swift 之类的封装。

---

## 3. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  SyncApp (SwiftUI, @main)                               │
│   ├── 同步任务 / 同步日志 / 设置                          │
│   ├── 菜单栏常驻项（后台同步状态）                         │
│   └── AppModel (@Observable, 主线程状态)                 │
└──────────────────────────┬──────────────────────────────┘
                           │ 同一进程内直接调用，无 FFI 边界
┌──────────────────────────▼──────────────────────────────┐
│  SyncEngine (Swift Package Library，不依赖 SwiftUI)      │
│   ├── DirectoryScanner   递归遍历 + 过滤 + 软链策略        │
│   ├── ContentHasher      CryptoKit SHA-256 流式 + 并发     │
│   ├── IndexStore         SQLite 三方索引（本地/远端/基线）  │
│   ├── Reconciler         三方比对 + 冲突裁决               │
│   ├── Transfer          分块传输 + 断点续传 + 原子落盘      │
│   └── StorageDriver[]    各存储后端适配器                  │
│        ├── LocalDriver   文件系统                        │
│        ├── SMBDriver     系统挂载点                      │
│        └── WebDAVDriver  URLSession                     │
└─────────────────────────────────────────────────────────┘
```

**分层约束**：`SyncEngine` 是纯逻辑层，**不 import SwiftUI**。
这条约束必须靠构建系统强制——SwiftPM 的 target 依赖关系天然做到这一点：
引擎 target 不声明对界面框架的依赖，误引入就编译不过。

这不是洁癖：引擎层能被无界面测试直接驱动，是整个验证策略的基础（见 9.3）。

---

## 4. 同步引擎设计

### 4.1 三种模式

| 模式 | 语义 |
|---|---|
| **双向同步** | 两端互为镜像；两侧都变则按冲突矩阵裁决 |
| **仅上传** | 本地为源，目标端只增不改；本地删除不传播 |
| **仅下载** | 目标端为源，本地只增不改 |

### 4.2 三方索引模型

索引库记录三个快照，而非两份：

| 快照 | 含义 |
|---|---|
| **本地** | 上次同步完成时本地端的状态 |
| **远端** | 上次同步完成时远端的状态 |
| **基线** | 上次同步**共同确认一致**的状态 |

**为什么必须三方**：只有两方时无法区分"本地新增了文件"与"远端删除了文件"——两者在差异列表里长得一样，但正确动作相反。基线记录的就是"上次一致"这一事实。

**远端侧要记录 `remote_id` 而不只是路径**：网盘的重命名是 id 不变、路径变；只看路径会把重命名误判成"删除 + 新增"，进而触发一次无意义的完整重传。

### 4.3 变更检测

```
变更候选 = 三方索引差异 ∪ FSEvents 通知
确认变更 = 内容哈希比对（对候选文件）
```

**两级设计的意义**：FSEvents 便宜但有噪音（自身写入也会触发），哈希准确但昂贵。用前者缩小范围、用后者确认，兼顾正确性与性能。

**哈希必须缓存**：按 `(路径, 大小, mtime)` 索引缓存哈希值。不做缓存的话每次同步都要重算全部内容——按实测数量级（十万文件 / 十几 GB）这是几十秒量级的纯浪费。

### 4.4 冲突裁决矩阵

| 本地 | 远端 | 动作 |
|---|---|---|
| 未变 | 未变 | 无操作 |
| 已变 | 未变 | 本地 → 远端 |
| 未变 | 已变 | 远端 → 本地 |
| 已变 | 已变（内容相同） | 无操作（内容一致，时间戳差异忽略） |
| 已变 | 已变（内容不同） | **冲突**，双方保留 |
| 已删除 | 未变 | 远端删除 |
| 未变 | 已删除 | 本地删除 |
| 已删除 | 已变 | **冲突**，保留远端（删除是最危险的操作） |
| 已删除 | 已删除 | 无操作 |

**冲突处理**：保留两份，冲突文件重命名为
`原名 (冲突-<设备名>-<日期>).ext`。
**`device_id` 必须现在就落库**（本机 UUID）——冲突双版本需要标注来源设备，事后补要处理历史数据迁移。

### 4.5 安全底线（不可协商）

1. **默认不删除目标端多余文件。**「同步删除」是独立开关，默认关闭。同类工具最高频的投诉就是误删。
2. **先落文件、后更新索引。** 保证崩溃后重跑幂等，不会丢数据。
3. **写入采用「临时名 → 原子重命名」。** 避免同步中断留下半截文件被当成完整文件。
4. **大批量删除需二次确认。** 超过阈值（如 100 个文件或总量的 10%）时暂停并提示。
5. **路径逃逸防护。** 任何写入路径必须校验落在同步根目录内，且**目标自身也要校验**（不只看父目录）——目录内一个指向外部的软链接即可绕过父目录校验。

### 4.6 断点续传

- 大文件按固定块大小分块，逐块计算哈希
- 中断后重传时，只重传哈希不匹配的块
- 块级哈希与文件的整体哈希同时记录：前者用于续传，后者用于一致性确认

### 4.7 存储驱动层（本地 / SMB / WebDAV）

三种目标端的实现差异极大：一个是 `FileManager`，一个是"先挂载成目录再走 FileManager"，
一个是 HTTP 上的六个自定义方法。但**对上层它们必须是同一件事**，
否则同步逻辑要写三遍，且三遍的行为会随时间漂移。

因此引擎里定义 `StorageDriver` 协议，只暴露"以相对路径为坐标的六个操作"，
坐标原点由驱动自己决定：

```
probe()                                  连接测试（必须真实往返）
list(relativePath:)                      列一级条目
makeDirectory(relativePath:)             建目录（含中间层级）
upload(from:to:) / download(_:to:)        上传 / 下载
remove(relativePath:recursive:)          删除（非递归时拒绝删非空目录）
```

#### 4.7.1 SMB：交给系统挂载，之后当本地目录用

macOS 自带的 SMB 客户端由 **NetFS 框架**驱动，就是访达"连接服务器"用的那套，
已处理协商版本、NTLM/Kerberos 认证、服务端方言差异。自己实现必然是几千行
且长期有兼容性坑。

所以 SMB 驱动 = **把共享挂上，然后所有读写走挂载点，复用 `LocalFileDriver`**。
这也是 SMB 支持只需要两百行的原因，且读写语义与本地目录天然一致
（不会出现"本地能传、SMB 传不了"的偏差）。

Swift 无法直接 `import` C 框架（没有 `.swiftmodule`），故用 SwiftPM 的
`systemLibrary` 目标提供 module map。**模块映射必须写 `link framework "NetFS"`**：
写 `link "NetFS"` 会让链接器去找 `libNetFS.dylib`，直接报 `library 'NetFS' not found`。

两个必须记住的实测行为：

1. **`NetFSMountURLSync` 会阻塞且无超时。** 实测对不可达地址调用它会一直不返回，
   还会拉起 `NetAuthAgent`（系统认证代理）。因此**只用异步挂载 + 主动取消**
   （`NetFSMountURLAsync` / `NetFSMountURLCancel`），由调用方施加超时。
2. **`kNAUIOptionNoUI` 必须设。** 不设的话缺凭据时系统会弹认证对话框；
   对后台同步任务来说，弹窗等于永久挂起。

挂载必须**串行化并缓存**（`SMBMountCoordinator`，actor）：同一共享被两个任务指向时，
各挂各的会得到 `/Volumes/Share` 与 `/Volumes/Share-1` 两个挂载点 —— 操作同一份数据
却以为是两个目标。同时**必须以系统返回的挂载点为准**，不能自己拼 `/Volumes/<共享名>`。

#### 4.7.2 WebDAV：URLSession + 自写 multistatus 解析

WebDAV 就是在 HTTP 上加了几个自定义方法，而 `URLSession` 允许任意 `httpMethod`，
所以不需要任何第三方库 —— 换来的只是几百行代码，却省掉一项供应链风险。

关键点：

- **集合的 URL 必须以 `/` 结尾。** 很多服务端用尾斜杠区分"集合"与"成员"，
  少了它 MKCOL 会建出一个同名文件，或 PROPFIND 返回单条而非子项列表。
- **`MKCOL` 不像 `mkdir -p`**：父集合不存在会直接失败，所以逐级创建；
  已存在时服务端回 405，必须视为成功（幂等），否则重跑同步直接失败。
- **解析 multistatus 必须按 `namespaceURI + 局部名` 匹配。** 硬编码找 `d:response`
  会在服务端用 `D:`（前缀只是别名）或用默认命名空间 `xmlns="DAV:"`（元素名无前缀）
  时全部失配。
- **必须按 `propstat` 分组后再合并。** 一个 response 里可以有多个 propstat（成功/失败分组），
  而 `<status>` 出现在 `<prop>` **之后**。边读边写会让失败分支的空值覆盖成功分支的真实值，
  表现为"文件大小全是 0"。
- **删除自己递归**，不指望服务端能删非空集合：RFC 4918 明确允许服务端返回 409/423，
  依赖它"应该能删"会在不同 NAS 上表现不一致。

#### 4.7.3 共用的安全底线

- **路径逃逸防护要过两道**：词法检查（消掉 `..`）**加上**软链检查，
  且软链检查要校验**目标自身**——根目录里一个指向外部的软链，词法上完全正常。
- **上传一律"先写临时名、再替换"**，避免崩溃留下残文件（其 mtime 与大小看起来都正常，
  下次同步会认为"内容没变"，坏文件就永久留下了）。
  替换优先用 `replaceItemAt`（原子），**但失败必须能降级**：它底层是
  `renamex_np(RENAME_SWAP)`，**smbfs 上未必支持**，这类失败只在挂载到网络卷时才出现。
- **非递归删除遇到非空目录必须报错**，而不是静默递归。
  静默递归删除是数据丢失最常见的形式。

---

## 5. macOS 平台适配

### 5.1 App Sandbox：升级为关键决策

转向前"是否上架"只是一个待决问题；**原生开发后它直接决定文件访问的架构**。

| 分发方式 | 沙盒 | 文件访问方式 |
|---|---|---|
| **Mac App Store** | 必须开启 | 用户经 `NSOpenPanel` 选择目录 → **持久化安全作用域书签** → 每次访问前 `startAccessingSecurityScopedResource()` |
| **官网下载 / 自行分发** | 可关闭 | 直接按路径访问；但需自备签名与公证 |

**关键点：仅保存路径字符串是不够的。** 沙盒下应用重启后就失去该目录的访问权，必须保存书签数据（`URL.bookmarkData(options: .withSecurityScope)`）并在每次访问时解析。

**建议**：从第一天就按"沙盒可用"设计文件访问层——把"取到根目录的安全作用域 URL"抽象成一个入口。这样无论最终走哪条分发路径，都不需要重写引擎。

### 5.2 菜单栏常驻

同步工具需要在窗口关闭后继续工作。做法：

- `MenuBarExtra` 提供常驻状态项
- 关闭主窗口时最小化到菜单栏（而非退出）
- 由 SwiftUI 的 `WindowGroup` 创建的窗口**已经有 delegate**（实测类名 `AppKitWindowController`），接管时必须**转发**未处理的消息，否则会静默打断 SwiftUI 的窗口管理；且 `NSWindow.delegate` 是弱引用，必须强引用原 delegate，否则转发链断掉

### 5.3 后台执行

- 应用内：同步任务在 `Task` 中执行，不阻塞主线程，进度经主线程回写
- 应用未启动时：LaunchAgent（`SMAppService.agent`）可选，但**默认不做**——常驻后台服务会显著提高审核与用户信任门槛

### 5.4 文件系统注意事项

| 事项 | 处理 |
|---|---|
| 外接盘 FAT/exFAT | mtime 精度 2 秒，**不可用于变更判定** |
| 大小写不敏感卷 | 索引键统一按 Unicode NFC 规范化并小写比较 |
| 资源分叉 / 扩展属性 | 用 `FileManager.copyItem` 保留；不要用裸 `read/write` |
| 软链接 | 一律不跟随（避免目录环与重复计数） |
| 正在写入的文件 | 用 `NSFileCoordinator` 协调；或按 mtime 稳定性判定 |

---

## 6. 界面设计

### 6.1 结构

`NavigationSplitView` 三段式：

```
┌──────────┬────────────────────────────────────────┐
│ 侧边栏    │ 内容区                                  │
│          │                                        │
│ 同步任务  │  任务卡片列表 / 执行进度 / 文件明细        │
│ 同步日志  │  分级日志，可按任务与级别筛选              │
│ 设置     │  存储连接 / 同步参数 / 引擎自检            │
└──────────┴────────────────────────────────────────┘
```

用 `NavigationSplitView` 而非自绘侧栏：它自动处理侧栏折叠、键盘导航、以及系统外观变化。

内容区**不再有常驻的引擎状态条**（曾显示 `引擎版本 · 构建配置 · 哈希并发`）。
移除后引擎加载失败的可见性下降，这条信息现在只出现在设置页的「引擎」区块与日志里；
若将来希望"出问题时才提示"，正确做法是**仅在 `engineError != nil` 时**插入警告条，
而不是恢复常驻条。

### 6.1.1 内容必须显式顶部对齐

```swift
.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .top)   // alignment 不能省
```

`frame` 的默认对齐是 `.center`，而各页内容高度是**固有**的
（`ContentUnavailableView` 不会撑满可用空间，设置页的 `ScrollView` 才撑满）。
于是任务页的页头「同步任务 0 个」与右上角按钮会**浮在窗口正中间**——
实测修复前在 y≈300（窗口高 680），看起来像布局坏了。

这类问题的判据很干净：**取内容区最上面一行墨迹的 y 值**。
顶部对齐后是小值（实测 69），居中时会接近窗口高度的一半。

### 6.1.2 侧栏排版

侧栏条目的字号与行距**显式指定，不吃 `List` 默认值**（默认约 13pt、行距偏紧，
三个入口缩成一坨）：

| 项 | 值 |
|---|---|
| 文字 / 图标字号 | 15pt |
| 每行额外垂直内边距 | 6pt（只放大字号会让行与行贴得更紧） |
| 侧栏列宽 | min 186 / ideal 206 / max 260 |

字形实测：图标高度 13.0 → **15.5pt**，条目行距 32.5 → **40.0pt**。

**注意侧栏列宽会被持久化**：`NSSplitView` 把子视图框架存进应用偏好
（`NSSplitView Subview Frames … SidebarNavigationSplitView`），
**存过之后 `ideal` 就不再生效**。字号调大后要放宽列宽时，
若用户机器上已有旧值，需要一并清掉那个偏好键，否则看到的仍是旧宽度。

### 6.2 状态管理

单例 `@Observable final class AppModel`，注入 `Environment`。

**不引入状态管理框架**：当前状态规模（任务列表 + 日志 + 设置）用 Observation 足够，
引入第三方框架的收益抵不上它的概念负担与依赖风险。等到出现复杂的跨页派生计算时再考虑。

### 6.3 主题与外观

必须同时支持浅色与深色，并允许三种模式：

| 模式 | 用途 |
|---|---|
| 跟随系统 | 默认。macOS 用户普遍开启"日落到日出自动切换"，强制固定会显得格格不入 |
| 浅色 | 手动指定 |
| 深色 | 手动指定 |

**两处都要设**，缺一不可：

```swift
NSApp.appearance = mode.nsAppearance(…)       // 管菜单、NSOpenPanel 等不隶属任何窗口的部件
.preferredColorScheme(model.effectiveColorScheme)   // 管 SwiftUI 绘制的内容（含窗口标题栏）
```

只设一处会得到"深色标题栏 + 浅色内容"这类割裂观感，且**不会报任何错** ——
只有在实际截图里才看得出来。

**已打开的窗口要逐个设置 `window.appearance`**，否则标题栏不会立即重绘
（只有新建窗口才会用上 `NSApp.appearance` 的新值）。

右上角提供便捷切换按钮，在浅色与深色之间直接切换。从「跟随系统」进入时，
切到**当前实际外观的反面** —— 若固定切到某个方向，系统恰好是深色时点一下会"毫无变化"。
想回到跟随系统，到设置页选择。

窗口标题为 **`sync-engine`**（`WindowGroup` 标题、`.navigationTitle`、
`MenuBarExtra` 标题、`Info.plist` 的 `CFBundleName`/`CFBundleDisplayName` 四处一致）。
标题由窗口服务器给出，验证时读 `kCGWindowName` 即可，不必看代码。

#### 6.3.1 「跟随系统」也必须解析成**具体**外观（`nil` 不能用）

直觉做法是给「跟随系统」返回 `nil`（"交给系统决定"），**但那样会在运行中切换时炸掉**：

> 从「浅色」切到「跟随系统」、系统为深色时，SwiftUI 的**详情列变成一块空白深色**，
> 且**一直停留**不再恢复。实测三处亮度：标题栏 0.806（浅）、侧栏 0.551（中）、
> 详情区 0.139（深且无任何内容），而强调色仍是浅色档 —— 四者互相矛盾。

静态启动（一开始就是「跟随系统」）反而是好的，所以这个坑**只在切换时**出现，
逐页点一遍很容易漏掉。

解法：`nil` 这个中间态压根不出现——

```swift
// AppearanceMode
func nsAppearance(systemIsDark: Bool) -> NSAppearance? {
    switch self {
    case .system: return NSAppearance(named: systemIsDark ? .darkAqua : .aqua)
    case .light:  return NSAppearance(named: .aqua)
    case .dark:   return NSAppearance(named: .darkAqua)
    }
}
```

于是"切到跟随系统"退化成普通的「浅色 → 深色」，两边都能正常重绘。

代价是**系统在日落时自动切换就不会自动生效了**（系统不再替我们更新窗口），
所以必须自己补上：

```swift
DistributedNotificationCenter.default().addObserver(
    forName: NSNotification.Name("AppleInterfaceThemeChangedNotification"), …)
// 通知到达后稍等 200ms 再读偏好 —— 立刻读容易读到旧值
// 若当前是 .system：更新缓存的 systemIsDark → 重新 apply
```

判定系统明暗**不能用 `NSApp.effectiveAppearance`** —— 应用自己把外观固定成浅色时，
那个值反映的是我们的覆盖而不是系统的设置。读全局域的 `AppleInterfaceStyle` 才对。

`systemIsDark` 必须**缓存在 `AppModel` 里**（而不是每次现读 UserDefaults）：
现读的话 SwiftUI 不知道它变了，界面就不会重绘。

右上角的切换按钮同样要读模型的生效方案而不是 `\.colorScheme` 环境值 ——
两者平时一致，但**外观刚切换的那一瞬**环境值可能还没跟上，而那正是按钮要判断方向的时刻。

### 6.4 强调色

**品牌色 `#32CD32`（LimeGreen）**，与图标底色共用。界面强调色按明暗取值：

| 模式 | 值 | 对比度 | 说明 |
|---|---|---|---|
| 浅色 | `#22A322` | 约 **3.3:1**（白底） | 品牌绿**深一档**；达到非文字图形 3:1 的底线 |
| 深色 | `#32CD32` | 约 **7.9:1**（深底） | 品牌色本身，深底上好看又好读 |

浅色模式沿用品牌色本身只有 **2.12:1**——作为小字号文字（徽标、日志级别）明显偏低。
故浅色改用同色系深一档的 `#22A322`：色相一致、一眼仍是同一个绿，文字读起来清楚得多。
**这不叫"品牌色分裂"**：同一个品牌绿在两个背景明度下的两种呈现。

**应用图标底色始终是品牌色 `#32CD32`**（在 `Scripts/make_icon.swift` 里单独定义），
不随界面强调色变化：图标是品牌标识，界面强调色要受可读性约束。

界面实测：浅色渲染众数 `#22A321`、深色 `#33CD32`（与源码各差 1 个数值，来自显示色彩管理）。
验证方式：分别以两套主题截图取绿色像素众数——**并注意
系统红绿灯按钮里的那个绿（`#00BC00`）也是绿色像素**，它在活动窗口里有色、
非活动窗口里是灰的，统计时不要把它当成强调色。

界面实测：强调色实际渲染众数为 `#33CD32`（与源码差 1 个数值，来自显示色彩管理）。

#### 6.4.1 一处必须知道的坑：`Color.accentColor` 无视 `.tint()`

根视图上注入的 `.tint(Palette.accent(...))` **只影响 SwiftUI 控件**。
而 `Color.accentColor` 在 SwiftUI 里解析为**系统强调色**（`NSColor.controlAccentColor`），
它不理会 `.tint()`：

```swift
VStack { ... }.tint(limeAccent)                       // 按钮变绿
Image(systemName: "cpu").foregroundStyle(.accentColor)  // 这个图标仍是系统蓝
```

两者混用时界面上会同时出现两种颜色，**且不报任何错** —— 只有在实际截图里逐像素看才发现。

因此本项目**禁止使用 `Color.accentColor`**，改为：

- 定义一个 `appAccent` 环境值（`Support/AppAccent.swift`），由根视图按当前明暗注入，各处读它
- 侧栏条目不用 `Label`（`List` 会用系统强调色渲染其图标，`.tint()` 管不到），
  拆成图标 + 文字并显式着色
- 不用 `.buttonStyle(.link)` —— 该样式写死了系统的链接蓝

验证方式：截图后统计**异色像素数**，要求两套主题下均为 **0**。

#### 6.4.2 警告色：琥珀，不是红

警告色是 `Palette.warning = #B86B08`（琥珀），白底与深底各约 **4.1:1**。

这条经过两轮调整，值得记下推理过程：

- 强调色曾是**淡橙** → 警告色只能用红，否则两者无法区分；
- 结果日志列表里 **WARN 与 ERR 两个标签都是红色**，肉眼分不出来 ——
  这是"为避开撞色而引入"的真实缺陷；
- 强调色改为**绿**之后，橙色空出来了，于是把警告色恢复为琥珀，
  WARN 与 ERR 重新可区分。

结论：**强调色与警告色必须成对决定**，只改一个必然在别处留下问题。
两者都从 `Palette` 取，不各处写死。

注意：**验证颜色时不要做精确色值比对。** `NSColor` 的色彩空间换算结果与
SwiftUI 实际渲染值不一致（实测红通道差 0.12），逐像素等值比较会得到零匹配，
看起来像"颜色根本没画上去"。应改为按"色族"判定，见 11.5。

#### 6.4.3 环境值注入**到不了** `NavigationSplitView` 的侧栏

这是 6.4.1 的续集，也是同类里更隐蔽的一个。

```swift
NavigationSplitView {
    List(...) { item in
        Image(systemName: item.systemImage)
            .foregroundStyle(accent)        // ← 读注入的环境值
    }
} detail: { ... }
.environment(\.appAccent, Palette.accent(for: colorScheme))   // ← 注入在这里
```

**侧栏那一列拿不到这个注入值**（它由 AppKit 的分栏控制器承载），
于是退回 `AppAccentKey.defaultValue` —— 也就是**浅色强调色**。

失效方式极隐蔽：深色模式下侧栏图标显示深绿 `#22A321`，
而同一个窗口里其它强调色元素显示品牌绿 `#32CD32`。两套主题各错一半，**不报任何错**，
而且浅色模式下完全正确（因为回退值恰好就是浅色值），单看浅色根本发现不了。

判据：**分别取"侧栏图标"与"详情区强调色元素"的像素众数**，
要求两者在同一主题下相同、且随主题变化。

修法是不要依赖这条通道——在侧栏里**按当前明暗直接算**：

```swift
private var sidebarAccent: Color { Palette.accent(for: colorScheme) }   // colorScheme 是可靠的
```

`colorScheme` 在侧栏里是准的（文字颜色随主题正确变化即证明），只有自定义环境值传不进去。

### 6.5 菜单栏常驻

同步工具是"后台干活"的形态，窗口关掉后仍应能看到状态并唤回窗口。

- `MenuBarExtra` 提供常驻状态项：任务数、日志数、扫描状态、打开主窗口、退出
- 关闭主窗口时是否退出由用户选择（见 6.6）

### 6.6 关闭主窗口的两种行为

设置页提供「关闭主窗口时」选项：

| 选项 | 行为 | 实现 |
|---|---|---|
| 退出软件 | 关窗即结束进程 | `windowShouldClose` 返回 true；`applicationShouldTerminateAfterLastWindowClosed` 返回 true |
| 最小化到菜单栏 | 进程与菜单栏项保留，窗口隐藏 | `windowShouldClose` 返回 false 并 `orderOut`；`applicationShouldTerminateAfterLastWindowClosed` 返回 false |

**两处必须配套设置。** 只改一处会出现"窗口关了但进程还在"或"拦截了关闭却仍然退出了"这类不一致。
前者决定"窗口该不该关"，后者决定"窗口关掉之后应用要不要退"——是两件事。

接管 SwiftUI 窗口的 delegate 有两个必须处理的点：

1. **SwiftUI 的 `WindowGroup` 窗口本来就有 delegate**（实测类名 `AppKitWindowController`），
   不是 nil。所以不能用「delegate == nil 才挂」这种守卫，那样拦截永远挂不上。
2. **必须转发 + 强引用原 delegate。** `NSWindow.delegate` 是弱引用，
   不强引用的话原 delegate 会被立刻回收，转发链断掉；不转发的话 AppKit 会因为
   "本对象不响应"而静默跳过全部可选回调，把 SwiftUI 的窗口管理整个打断。

识别主窗口用**结构判定**而不是标题字符串（`WindowGroup` 的标题可能就是个空格，
还会随系统本地化变化），并排除面板与 sheet —— 否则会把"新建任务"表单也挂上拦截。

### 6.7 任务编辑

任务可随时修改。两个容易做错的地方：

1. **源目录变更后必须清除旧的预览结果。** 否则界面上会显示一份属于旧目录的统计数字，
   比不显示更糟 —— 用户会以为那是当前目录的数据。
2. **`await` 期间必须用 id 重新定位任务**，不能按下标写回。
   扫描是异步的，期间用户可能已经删除或插入任务，按下标写回会改到别的任务上。

新增与编辑共用同一个表单视图：字段、校验规则、目录选择逻辑完全一致，
分成两份必然会随时间漂移。

---

## 7. 安全与凭证

### 7.1 凭证存储（已实现）

| 类型 | 存储 | 说明 |
|---|---|---|
| SMB / WebDAV 用户名密码 | **Keychain**（`kSecClassGenericPassword`） | 不落文件 |
| 任务里的目标端配置 | 内存（后续落配置） | **不存密码**，只存用户名与地址 |
| 索引库 | `~/Library/Application Support/<bundle-id>/` | 不含凭证 |
| 配置导出 | 需加密，**不含凭证** | 凭证需在新设备重新输入 |

**凭证绝不出现在日志、崩溃报告、或配置导出文件中。**

具体做法：

- 记录以 `种类 + 规范化后的地址` 为账户名，值里整体带用户名、密码、证书策略。
  同一位置无论写成 `smb://nas/Share` 还是 `nas/Share` 都会落到**同一条记录** ——
  否则会出现"改了密码但另一个任务还在用旧的"。
- 写入用"先 `SecItemUpdate`、失败再 `SecItemAdd`"，**不是"先删后加"**：
  后者在两次调用之间崩溃会留下"凭据没了"的状态，用户会莫名其妙连不上。
- 任务结构里**不存密码**：任务会出现在日志、调试输出与配置里，密码一旦进去就到处泄漏。
  需要连接时才从钥匙串取出。
- 编辑任务时**密码框不回填**。哪怕用 `SecureField`，框里的内容也会随截图、录屏、投屏
  一起泄漏；而"编辑任务"通常只是想改名字或路径。留空表示"不改"，
  另设一个显式的"清除已保存的密码"。
- 删除任务时顺手删掉该目标端的钥匙串记录：留着就是一份没人会再用的无主敏感数据。

### 7.2 传输安全

两处需要显式放开，都会降低安全性，因此都在界面上给出提示：

| 项 | 决策 | 理由与缓解 |
|---|---|---|
| 明文 HTTP（WebDAV） | **允许** | 群晖/威联通出厂只提供 http 的 WebDAV 端点。地址以 `http://` 开头时编辑器给出明文传输提示 |
| 自签名证书 | **允许，但需显式勾选** | NAS 出厂即自签。勾选说明中写明"无法通过证书链确认服务器身份，中间人攻击无法察觉，只在可信局域网内开启"；探测结果里也会记录 |
| ATS | `NSAllowsArbitraryLoads = true` | 不开的话上述两类服务器会直接连不上，而用户看到的是"传输安全策略"错误，与"地址填错"难以区分 |

**缓解措施**：只对用户明确填写的地址发起连接，不做任何自动发现；
若将来定位为企业内网部署，应改为 `NSAllowsLocalNetworking` 并要求全部端点使用 https。

---

## 8. 日志体系

- 分级：`debug` / `info` / `warning` / `error`
- 落盘：`~/Library/Logs/<bundle-id>/sync-YYYY-MM-DD.log`
- 轮转：按日切分，保留 N 天
- **同时接入 `os.Logger`**，便于用 `log stream` 实时观察与系统诊断报告关联
- 界面内日志页只显示本次会话，历史查文件

---

## 9. 开发与验证

### 9.1 工程结构

```
SyncApp/                           SwiftPM 包
├── Sources/
│   ├── SyncEngine/                引擎（纯逻辑，不依赖 SwiftUI）
│   └── SyncApp/                   界面与入口
│       ├── SyncAppMain.swift      @main + --selfcheck 分流
│       ├── AppDelegate.swift      激活策略、外观、窗口接管
│       ├── State/AppModel.swift
│       └── Views/                 三个功能页
├── Tests/SyncEngineTests/         引擎单元测试（swift test）
├── Scripts/
│   ├── build_app.sh               组装 .app（含 Icon、Info.plist、签名）
│   ├── make_icon.swift            AppKit 绘制图标 → iconutil 合成 .icns
│   └── selfcheck/main.swift       无界面自检
└── dist/                          打包产物
```

### 9.2 为什么用 SwiftPM 而不是 Xcode 工程

| | SwiftPM（采用） | Xcode 工程 |
|---|---|---|
| 工程文件 | **纯文本，可 diff** | `.pbxproj`，冲突难解、手工编辑易错 |
| 命令行构建 | `swift build` | `xcodebuild` + scheme 配置 |
| CI 友好 | 高 | 中 |
| 目录结构 | 约定优于配置 | 显式维护 |
| 资源束 | 需手工组装 | 原生支持 |

界面层只用 SwiftUI（无 xib/storyboard），资源只有图标与 Info.plist，两者都可在打包脚本里生成——**Xcode 工程的唯一优势在此不成立**。

> 若将来上架 Mac App Store 需要 Xcode 工程，SwiftPM 包可被 Xcode 直接打开，
> 或用一个薄 Xcode 工程引用本包。现在不必为此付出 `.pbxproj` 的维护成本。

### 9.3 验证策略：分层，且不依赖肉眼

| 层 | 手段 | 覆盖 |
|---|---|---|
| 引擎逻辑 | `swift test` | 扫描、哈希、比对、索引 |
| 打包产物 | `--selfcheck` 参数 | **真实交付的二进制**能否跑通引擎 |
| 应用存活 | 启动后查进程 + `lsof` | 启动崩溃、动态库缺失 |
| 界面渲染 | 离屏渲染 + 位图哈希/墨迹比例 | 页面是否真的画出来、导航是否真的切换 |
| 交互 | 辅助功能 API / 真实鼠标事件 | 仅在授权可用时 |

**`--selfcheck` 的价值**：它让同一份二进制既可双击运行、又可被脚本驱动。
自检程序只能验证抽出来的纯逻辑；用交付产物本身跑，才覆盖真实打包路径。

**无人值守环境下的界面验证**：截图在无「屏幕录制」权限时只会返回壁纸，
不能据此判断界面没渲染。改用离屏渲染 + 统计位图特征（详见 11.2）。

### 9.4 构建脚本的已知坑

1. **SwiftPM manifest 沙箱**：若当前 shell 已被沙箱隔离，嵌套沙箱会失败，需 `--disable-sandbox`
2. **bash 3.2 与中文**：变量紧跟全角字符时必须写 `${VAR}`，否则报 `unbound variable`
   - 自查：`rg -n '\$[A-Za-z_][A-Za-z0-9_]*[^[:ascii:]]' Scripts/`
3. **图标缓存**：`CFBundleVersion` 不变则系统永远显示旧图标；每次打包用时间戳版本号并
   `lsregister -f` 失效缓存
4. **压缩 .app 必须用 `ditto`**：`zip` 会丢扩展属性，解包后签名失效
5. **不要用 `rm -rf` 清理旧 bundle**：某些环境的安全护栏会拦截，导致 `set -e` 脚本中断

---

## 10. 分发

| 方式 | 要求 |
|---|---|
| 本机 / 内部分发 | ad-hoc 签名（`codesign --sign -`）即可 |
| 官网下载 | **开发者证书签名 + 公证（notarize）**，否则用户双击会被 Gatekeeper 拦下 |
| Mac App Store | 开发者账号 + 沙盒 + 应用审核 |

**注意**：ad-hoc 签名的产物经网络传输后会带上 quarantine 属性，Gatekeeper 直接拦下。
**这不是 bug，也无法靠重新签名绕过**——需要真实证书。

---

## 11. 已确认的技术结论（沿用自前期实测）

以下结论与平台无关，在转向前已实测确认，继续有效。

### 11.1 Shell 脚本：macOS 自带 bash 3.2 不识别 UTF-8

`$VAR（` 这种"变量紧跟全角字符"的写法会把后续字节并进变量名，报
`VAR<乱码>: unbound variable`，脚本**静默中断**。

规则：变量引用紧跟非 ASCII 字符时一律写 `${VAR}`。

### 11.2 无人值守环境下验证 GUI（本机辅助功能权限被拒时）

1. `lsof -p <pid>` 查进程实际映射了哪些库/文件
2. `CGWindowListCopyWindowInfo` 取窗口编号与尺寸（**不需要**辅助功能权限；
   而 AppleScript 操作 System Events 需要，会报 -10004）
3. `screencapture -x -o -l<windowId>` **只截单个窗口**，避免拍到用户其他内容
4. **无「屏幕录制」权限时截图会返回只有壁纸的图**，看起来像界面没渲染，实际是被系统挡了。
   此时改用**离屏渲染**（`NSHostingView` + 永不显示的 `NSWindow`）后统计：
   - 位图**墨迹比例**（与区域中位亮度差 > 0.10 的样本占比）判断是否真的画了内容
   - 位图 **SHA-256** 对比两次渲染是否相同，判断某字段是否被绘制、导航是否真的切换
5. `sips -s format bmp` 转 BMP 后用 Python 标准库解析像素——**无需任何依赖**即可做分区色彩分析

### 11.3 无界面模式的线程陷阱

用 `Task { } + dispatchMain()` 驱动时，`@MainActor` 函数体内 `Thread.isMainThread`
**恒为 false**，任何 `NSWindow` 创建都会抛异常，而 `DispatchQueue.main.sync` 补救会**彻底死锁**。

正确做法是**自己泵主 RunLoop**：

```swift
while !finished { RunLoop.main.run(until: Date().addingTimeInterval(0.02)) }
```

### 11.4 持久化结构的字段兼容

给已有 `Codable` 结构新增字段会让**旧文件整体解析失败**（合成解码器对缺失键抛错，
`try?` 的结果是数据全丢）。新字段一律声明为 Optional，或手写
`init(from:)` 逐字段 `decodeIfPresent(...) ?? 默认值`。

### 11.5 颜色断言不要做精确比对

`NSColor.usingColorSpace()` 的换算结果与 SwiftUI 实际渲染值**不一致**
（实测红通道差 0.12，任何合理容差都卡不住）。分两层验证：
令牌层用 sRGB 精确比对；渲染层只判「色族」（如蓝 > 绿 > 红且足够亮）。

---

## 12. 路线图

| 阶段 | 目标 | 交付 | 验收 |
|---|---|---|---|
| **P0 骨架** ✅ | 可运行的 native 应用 | SwiftPM 工程、SwiftUI 三功能区、Swift 引擎（扫描 + SHA-256 + 并发哈希）、打包脚本、`--selfcheck`、`--bench` | **已完成**：`swift test` 13/13；`.app` 可双击运行；打包产物自检 8/8；关闭行为探针 10/10 |
| **P3 网络存储** ✅ | 差异化 | 存储驱动抽象层、`LocalFileDriver`、`SMBDriver`（NetFS 挂载）、`WebDAVDriver`（URLSession）、Keychain 凭据、任务编辑器按类型配置 + 真实「测试连接」 | **已完成**：测试 76 项全绿（含对进程内真实 WebDAV 服务端的完整往返、对真实 SMB 挂载的只读验证）；打包产物自检 15/15 |
| **P1 引擎核心** | 真正的同步能力 | SQLite 三方索引、Reconciler、分块传输与断点续传、冲突裁决 | 冲突矩阵全用例；崩溃恢复；幂等重跑 |
| **P2 变更检测优化** | 可用规模 | FSEvents 接入、哈希缓存（按 `路径+大小+mtime` 索引）、并发度自适应、网络卷自动降并发 | 十万文件级增量同步耗时较全量显著下降 |
| **P4 体验完善** | 可交付 | 日志落盘与轮转、配置导出导入、任务批量操作与持久化 | — |
| **P5 分发** | — | 开发者证书签名 + 公证；DMG；按 5.1 决策决定是否沙盒化、是否上架 | Gatekeeper 通过 |

**P0 与 P3 已完成**（P3 提前，因为"目标端能不能连上"直接影响 P1 传输层的数据模型设计）。
实测数据见第 13 节。

**注意 P3 只做到"连接与读写原语"，不等于"能同步"。** 目前具备的是：
探测、列目录、建目录、上传单个文件、下载单个文件、删除。
**尚不具备**：三方比对、增量判定、冲突裁决、断点续传、并发传输 —— 全在 P1。
界面上对此有明确标注，不把"能连上"说成"能同步"。

---

## 13. 环境与实测记录

### 13.1 本机环境

| 项 | 版本 |
|---|---|
| macOS | 27.0（26A428） |
| Xcode | 26.6（Build 17F113） |
| Swift | 6.3 |
| 外观 | 深色模式 |

### 13.2 P0 实测数据：Swift 引擎（当前实现）

测试对象：91,743 个文件 / 10,158 个目录 / 6.3 GB（`command-line-tools` 目录树，
含大量 node_modules 小文件，与真实用户目录的形态接近）。

**并发度对照（同一目录，release 构建）：**

| 并发度 | 哈希耗时 | 哈希吞吐 | 相对串行 |
|---|---|---|---|
| 1（串行） | 22.66 s | 283 MB/s | 1.0× |
| 4 | 8.03 s | 799 MB/s | 2.8× |
| 10 | **5.45 s** | **1179 MB/s** | **4.2×** |

遍历阶段在并发 4 以上稳定在约 1.6 s，不再是瓶颈。

**与前代 Rust 实现的对照：**

| 实现 | 哈希算法 | 并发 | 吞吐 |
|---|---|---|---|
| Rust（已删除） | BLAKE3 | 串行 | 约 323 MB/s |
| **Swift（当前）** | **SHA-256 (CryptoKit)** | **10 路** | **1179 MB/s** |

即：**改写为 Swift 后哈希吞吐提升约 3.6 倍**。主要贡献来自并发，
其次是 CryptoKit 的硬件加速 SHA-256。

**这项数据对架构的直接意义：**

1. **并发是本工作负载最有效的优化**。串行时每文件一次 open/read/close 的
   系统调用开销占主导，多核并行能把等待时间重叠掉。
2. **10 核的加速比只有 4.2 倍，不是线性**。说明存在竞争 ——
   最可能是磁盘 IO 带宽与文件系统元数据锁。继续加并发收益会迅速衰减，
   真正的下一步是**哈希缓存**（跳过未变化文件），而不是继续调并发度。
3. 由此确认技术方案 4.3 的两级变更检测设计是对的：
   FSEvents 缩小候选范围 + 内容哈希确认，缺一不可。

> 单次测量，未做多次取样与统计。作为量级判断足够；作为回归基线
> 需要在固定环境下重复测量并记录中位数。
>
> 复现命令：`SyncApp --bench <目录> [并发数]`

### 13.3 前代实现的历史数据（已随平台收敛删除）

转向前的 Rust + Flutter 实现曾对 143,636 个文件 / 18,631 个目录 / 13.2 GB
测得：遍历 4.22 s、哈希 37.54 s、合计 41.77 s、吞吐约 323 MB/s。
当时的结论（遍历仅占 10%、哈希占 90%、瓶颈是系统调用而非哈希算法）
在改写为 Swift 后依然成立，并直接指导了并发哈希的设计。

### 13.4 目标端存储的验证记录（2026-09-13）

#### 测试规模

| 套件 | 项数 | 内容 |
|---|---|---|
| `EngineTests` | 14 | SHA-256 官方向量、流式分块、扫描语义、软链策略、计时不变量、**软链根目录回归** |
| `StorageTests` | 29 | 地址规范化、路径逃逸（含软链逃逸）、本地读写往返、删除语义、钥匙串往返、日期与状态码解析 |
| `WebDAVDriverTests` | 22 | 对**进程内真实 HTTP 服务端**的完整往返 |
| `SMBDriverTests` | 11 | 对**本机真实 SMB 挂载**的只读验证（无挂载时跳过） |
| 打包产物自检 | 16 | 引擎 + 存储驱动接线 + NetFS + 钥匙串 + 启动态 |

#### WebDAV：真实 HTTP，不是 mock

服务端是测试进程内的 `MiniWebDAVServer`（`Network` 框架 + 真实磁盘后端），
所以能抓住协议层面的错误，而这些恰恰是 mock 测试永远抓不到的：

- 完整往返：`MKCOL` 建多级集合 → `PUT` 上传 → `PROPFIND` 列表 → `GET` 下载 → `DELETE` 递归删除
- **字节级校验**：往返后 SHA-256 与源文件一致（负载含二进制 + 中文 + 换行）
- **含中文与空格的文件名**往返 —— href 的百分号编码是必然出错的点，
  只有文件名真的是中文时才会暴露
- 错误链验证：错误密码 → `unauthorized`；不存在的集合 → `notFound`；非空集合非递归删除 → 报错**且内容原样还在**
- 协议正确性：断言用到的动词恰好是 `MKCOL/PUT/PROPFIND/GET/DELETE`，且**未出现 `POST`**
- 解析器的命名空间无关性：分别用 `D:` 大写前缀与默认命名空间 `xmlns="DAV:"` 各测一遍；
  另测"失败 propstat 不得覆盖成功 propstat"

#### SMB：真实挂载点，只读

本机已挂载多个局域网 NAS 共享，因此可以做真实验证：

- `statfs` 的 `f_fstypename` 判为 `smbfs`（**不靠路径前缀猜**）
- 由挂载来源反解主机/共享名并与用户输入的地址匹配成功；
  共享名不匹配或主机无关时**必须不匹配**（宽松到只比主机就会把数据同步到错误的共享）
- 驱动复用已有挂载点（`resolvedLocation` 等于系统挂载点），未挂出 `-1` 后缀的新卷
- 经驱动下载的文件与直接读挂载点的 SHA-256 一致
- **不可达主机必须在超时内快速失败**（实测 0.9 s），验证我们确实走的是异步挂载 + 取消

**写入未对用户的 NAS 执行。** 那会向真实共享写文件；改为提供一条由环境变量显式开启的
集成用例（`SYNC_SMB_WRITE_TEST`），并保证只在一个独立子目录内写、结束时删除。
本轮的写入路径由 `LocalFileDriver` 的测试覆盖 —— SMB 挂载后走的正是同一条代码路径。

#### 界面

两套主题各一张截图，且**两张图必须在同一状态下取**（同一页、窗口均为活动态）。
下表为最终值：

| 项 | 实测 |
|---|---|
| 浅色主题平均亮度 | 0.979 |
| 深色主题平均亮度 | 0.150 |
| 强调色实际渲染众数（浅 / 深） | `#22A321` / `#33CD32`（源码 `#22A322` / `#32CD32`） |
| **异色（非绿非中性）像素** | **浅色 0 / 深色 2** |
| 应用图标底色 | `#32CD32`（直接读 `.icns`，不经屏幕色彩管理） |
| **侧栏图标字形高度** | 13.0 → **15.5pt**（字号 12.5 → 15pt） |
| **侧栏条目行距** | 32.5 → **40.0pt** |
| **内容顶部对齐** | 内容区最上面一行墨迹 y：居中时 300 → 顶部对齐后 **69** |
| **常驻引擎状态条** | 已移除：详情区顶部条底色由 `rgb(250,253,250)`（8% 强调色底）变为 `rgb(254,254,254)`（纯背景） |
| 编辑 / 删除图标字形 | 12×12 / 14.5×16.5 pt |

"异色像素为 0"是界面验证最有价值的一条：它一次性证明了
**所有** SwiftUI 与 AppKit 绘制的元素都已统一（见 6.4.1）。
本轮它又抓到一次：侧栏图标因环境值注入失效而用了另一档绿（见 6.4.3）。

"实心强调色控件数为 0"是"启动无任务"的**图像侧证据** ——
它与自检里的 `启动时任务列表为空` 互相独立，一个验数据、一个验渲染。

#### 启动状态的确定性（两轮才修对的一个验证学问题）

这里连续踩了两次，值得完整记下：

**第一轮**：症状是同一主题连续启动，截出来的图内容不同。修法是启动后显式赋值覆盖
恢复值（`RootView` 的 `.task`），并关掉窗口状态恢复。

**第二轮**（本轮）：又观察到启动落在「同步日志」页。原因是那**一次赋值跑在竞速的输侧**——
侧栏（AppKit 承载的 List）会在窗口安定下来时把它自己的选中项推回绑定，
时序上晚于 `task` 的首次赋值。现在改成**设两次**（第二次留 300ms 让侧栏先安定），
并把这层道理写在代码注释里，免得后来者当它是冗余。

同时纠正一个**方法论错误**：上一轮用来判定"落在哪一页"的指纹是
"有没有 34×16 的外观色块"，它只能区分出「设置页」，把「同步任务页」与「同步日志页」
混为一谈 —— 于是"4 次全部落在同一页"这个结论其实是不成立的。
换成**有区分力的指纹**后判据才可靠：

| 页 | 可判据的特征 |
|---|---|
| 同步任务 | 页头「同步任务 N 个」+ 右上角「新建任务」按钮 |
| 同步日志 | 右侧分段筛选控件（选中段为强调色实心块）+ 左端 `INFO` 徽标 |
| 设置 | 「外观」区块里两个 34×16 强调色色块 |

判据要**取自该页独有**的东西，不能取自多页共有的东西。
现在截图脚本会先断言"选中项 = 同步任务 且 详情页 = 任务页"，通过才采用该图 ——
**截图前的状态断言应当是流程的一部分，而不是事后分析。**

#### 截图对比的一个陷阱：活动 / 非活动窗口

同一份代码，窗口是否 key，渲染不同：

| | 活动窗口 | 非活动窗口 |
|---|---|---|
| 按钮文案 | 强调色（`.borderless` 风格） | 灰色，且出现边框底 |
| 标签文字 | 正常 | 明显变浅（细体标签会浅到越过判定阈值） |
| 红绿灯 | 有色（绿为 `#00BC00`） | 全灰 |

**跨这两种状态比较截图会产生大量"幽灵差异"** ——本轮一度把非活动态下的
灰色按钮当成了"按钮文案颜色不对"。取对照图时必须保证窗口状态一致
（脚本里用 `osascript … activate` 显式激活，并留出 1–2 秒）。

#### 验证方法自身的漏洞：静态启动验不了运行中切换

「跟随系统显示错乱」这个 bug 之所以拖到用户报出来才发现，根因在验证方法：
此前每一轮都只验证**静态启动**（以某模式启动 → 截图），而故障恰好只在
**运行中切换**时出现，静态那条路完全正常。

补上之后的方法是一条矩阵（`Scripts/verify_appearance.sh`）：

| 场景 | 断言 |
|---|---|
| 静态启动 × 浅色 / 深色 / 跟随系统 | 标题栏、侧栏、详情区三处主题一致；强调色符合该模式；**详情区墨迹数不为 0**（防止"空白但底色对"骗过判据） |
| 运行中切换 × 浅色→跟随系统 / 深色→跟随系统 / 跟随系统→浅色 | 同上 |

运行中切换无法从外部点按（本机辅助功能权限被拒，UI 脚本报 -10004），
所以加了一个**走与应用相同入口**的诊断钩子：`--appearance-transition-test [模式]`
直接调 `AppModel.setAppearance`（与设置页点选同一个方法，不复制逻辑）。
它把日志写进文件而不是 stdout —— GUI 应用被 `open` 启动时 stdout 不可捕获，
而直接执行二进制虽然能捕获、窗口却**不一定出现**（实测两种行为都遇到过）。

经验：**"只在某个操作路径上出现"的缺陷，必须把那条路径本身做成可复现的**，
否则静态验证再密也只是在验另一条路。

#### 一个工具层面的坑：`grep` 匹配不了二进制里的中文

用 `strings` 检查"旧名字还在不在"会得到**假通过** —— macOS 的 `strings`
默认只输出 ASCII 序列，中文一律被丢弃；用 `grep` 匹配中文也不行（实测同一份二进制里
ASCII 模式能匹配、中文模式恒为 0）。**检查二进制内容要用 ASCII 哨兵**
（例如新增代码里的 `AppleInterfaceStyle`、`AppleInterfaceThemeChangedNotification`），
或者干脆比对文件时间戳确认"这份产物确实是新编译的"。

顺带：SwiftPM 偶发出现"源文件已改、`swift build` 却报 `Build complete! (0.19s)`
且不编译"的情况。判据同样是哨兵字符串 —— 没有哨兵就以为改动生效了。

---

## 14. 风险登记表

| 编号 | 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|---|
| R1 | 双向同步误删用户数据 | 中 | 极高 | 默认不删除；软删除进回收站；大批量删除二次确认（见 4.5） |
| R2 | 沙盒下文件访问授权丢失 | 中 | 高 | 安全作用域书签 + 每次访问前显式开启；把"取根目录 URL"抽象为单一入口（见 5.1） |
| R3 | 分发给他人时被 Gatekeeper 拦下 | 高 | 中 | 开发者证书签名 + 公证；发布说明中写清解除方式（见第 10 节） |
| R4 | 大目录同步耗时超出预期 | 中 | 中 | 并发哈希 + 哈希缓存 + 增量检测；已量化基线（见 13.2） |
| R5 | 索引库损坏导致同步行为异常 | 低 | 高 | 先落文件后写索引；索引可重建；定期校验 |
| R6 | SMB / WebDAV 服务端行为差异 | 中 | 中 | 覆盖常见服务端（群晖、威联通、Nextcloud、macOS 自带）；对异常退化而非失败 |
| R7 | 外接盘 mtime 精度导致误判 | 中 | 中 | 变更判定一律用内容哈希，mtime 只用于缩小候选范围（见 4.3） |
| ~~R8~~ | ~~鸿蒙端 Rust + Flutter 无法打通~~ | — | — | **已消除：平台取消** |
| ~~R9~~ | ~~夸克网盘合规风险~~ | — | — | **已消除：取消接入** |
| ~~R10~~ | ~~iOS 后台限制导致预期落空~~ | — | — | **已消除：iOS 取消** |

---

## 15. 待决策项

1. **是否上架 Mac App Store？** 决定沙盒是否开启，进而决定文件访问层的实现（见 5.1）。
   建议：**先按"沙盒可用"设计，但暂不开启**——架构留出空间，分发方式留到有用户时再定。
2. **是否需要菜单栏常驻？** 影响"关闭窗口 = 退出还是隐藏"的交互，以及是否要 LaunchAgent。
3. **目标用户是个人还是团队？** 团队场景会引入共享配置、权限模型、审计日志，与当前单人工具的形态差异较大。
